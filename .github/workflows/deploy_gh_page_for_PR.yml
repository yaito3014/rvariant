name: Deploy for PR

on: pull_request

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p public/${{ github.event.number }}
          cp -r doc/* public/${{ github.event.number }}/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: public/

  deploy:
    needs: upload

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages-${{ github.event.number }}
      url: ${{ steps.deployment.outputs.page_url }}${{ github.event.number }}/rvariant.html

    runs-on: ubuntu-latest
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
